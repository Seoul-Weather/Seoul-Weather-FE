import { getTemp } from "@/hooks/api";
import { useQuery } from "@tanstack/react-query";
import ApexCharts from "react-apexcharts";

export const Chart = ({ isLoading, data }: any) => {
    return null;
};
